- name: destroy launch config
  ec2_lc:
    name: "{{ aws_resource_tags.Env }}-{{ aws_resource_tags.Product }}-{{ aws_resource_tags.App }}-{{ aws_resource_tags.Process }}-{{ aws_resource_tags.Release }}"
    state: "absent"
    region: "{{ region }}"
